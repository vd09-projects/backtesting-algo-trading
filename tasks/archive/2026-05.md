# Task Archive — 2026-05

Completed and cancelled tasks from this month.

---

### [TASK-0098] Strategy — `PriceExit` wrapper: fixed stop-loss and target-profit

- **Status:** done
- **Completed:** 2026-05-11
- **Priority:** high
- **Created:** 2026-05-10
- **Source:** session
- **Context:** Engine had `TimedExit` for time-based exits but no price-based equivalent. Marcus ruled fixed SL and TP are needed for intraday strategies (ORB, Gap-and-Go) and must NOT affect MACD or any existing daily-bar strategy. Zero values = disabled — existing strategies unaffected.
- **Acceptance criteria:**
  - [x] `pkg/strategy/price_exit.go`: `PriceExit` struct implementing `Strategy` interface; fields `inner Strategy`, `stopLossPct float64`, `targetProfitPct float64`, `entryPrice float64`, `inPosition bool`
  - [x] `NewPriceExit(inner Strategy, stopLossPct, targetProfitPct float64) Strategy` constructor; zero value for either pct = that guard disabled
  - [x] `Next(candles []model.Candle)`: on BUY from inner, record `entryPrice = bar.Close`; on subsequent bars, if `bar.Close <= entryPrice*(1-stopLossPct)` emit SELL; if `bar.Close >= entryPrice*(1+targetProfitPct)` emit SELL; inner SELL always passes through and resets state
  - [x] SL and TP checked before delegating to inner — price-based exits take priority
  - [x] `Name()` returns `"price-exit(" + inner.Name() + ")"` matching `TimedExit` naming convention
  - [x] `Lookback()` and `Timeframe()` delegate to inner
  - [x] Golden tests: SL fires (+ exact boundary), TP fires (+ exact boundary), neither fires (inner exits), re-entry after SL reset, both disabled (zero pct), only SL enabled, only TP enabled
  - [x] `go1.25.0 test -race ./pkg/strategy/...` passes
  - [x] `golangci-lint run ./pkg/strategy/...` passes
  - [x] Tests written before implementation (TDD)
- **Notes:** TDD: compile-fail on test file confirmed before implementation. 10 tests total (3 metadata + 7 golden), 100% coverage, 0 lint issues. Guard condition uses `> 0` (not `!= 0`) — negative values treated as disabled. Exact-boundary TP test uses 20% (100*1.20=120.0 exact float64) because 100*(1+0.10) has float64 rounding error. Inner.Next() not called on SL/TP-exit bars (inner-state desync documented in godoc as harmless under no-pyramiding). Perspective review: APPROVE (0 blocking). Follow-up tasks: TASK-0105 (negative pct doc), TASK-0106 (fold-isolation test). Unblocks TASK-0074 (ORB) and TASK-0075 (Gap-and-Go) from PriceExit dependency.

---

### [TASK-0073] Tooling — end-to-end automated evaluation pipeline (`cmd/evaluate`)

- **Status:** done
- **Completed:** 2026-05-10
- **Priority:** medium
- **Created:** 2026-05-04
- **Source:** session
- **Context:** Running the full evaluation pipeline (universe sweep → walk-forward → bootstrap) currently requires manual handoff between three CLIs. A single CLI that runs the full sequence, writes structured outputs to a dated results folder, and produces a summary verdict removes all manual steps. Gates and methodology remain unchanged.
- **Acceptance criteria:**
  - [x] `cmd/evaluate/main.go` CLI: flags `--strategy`, `--params` (key=value pairs), `--universe`, `--timeframe`, `--from`, `--to`, `--out-dir`
  - [x] Runs full sequence: (1) universe sweep with DSR gate, (2) walk-forward on survivors, (3) bootstrap on walk-forward survivors
  - [x] If universe sweep produces zero survivors, pipeline halts immediately and writes `verdict.json` with `"result": "killed_at_universe_gate"` — does not proceed to walk-forward
  - [x] Each stage writes outputs to `--out-dir/YYYY-MM-DD-{strategy}-{timeframe}/` in same format as existing CLIs
  - [x] Summary `verdict.json` written at end: lists survivors with gate results, kills with stage and reason
  - [x] Existing gate thresholds unchanged — no new methodology; parameter search is a separate CLI (TASK-0077)
  - [x] Tests written before implementation (TDD)
- **Notes:** Owner: Priya (dev). 838-line main.go, 992-line test file. 28 tests. Quality gate: 0 lint issues, 0 races, 65% coverage (integration-only gap). Perspective review: APPROVE (0 blocking, 11 suggestions). Follow-up tasks created: TASK-0102 (collectTrades redundancy), TASK-0103 (makeStageDir MkdirAll), TASK-0104 (applyWFGate traceability). Unblocks TASK-0077.

---

### [TASK-0101] Data — re-run `cmd/fetch-history` for 10 previously failing 5-min instruments

- **Status:** done
- **Completed:** 2026-05-10
- **Priority:** high
- **Created:** 2026-05-10
- **Source:** session
- **Context:** TASK-0100 fixed the OHLC bad-candle hard-fail in `cmd/fetch-history`. The 10 instruments that previously failed at candle[450] (HDFCBANK, ICICIBANK + 8 midcap: ABCAPITAL, GODREJCP, SUNDRMFAST, ENDURANCE, LTTS, ALKEM, RATNAMANI, LEMONTREE) can now be fetched. Each will have 1 candle skipped and ~98,900 valid candles cached.
- **Acceptance criteria:**
  - [x] Run `cmd/fetch-history --universe universes/nifty50-large-cap.yaml --timeframe 5min --from 2021-01-01 --cache-dir .cache/zerodha` — HDFCBANK and ICICIBANK succeed (with 1 skipped candle each, logged in manifest)
  - [x] Run `cmd/fetch-history --universe universes/nifty-midcap-liquid.yaml --timeframe 5min --from 2021-01-01 --cache-dir .cache/zerodha` — all 8 midcap failures succeed
  - [x] `fetch-progress.json` manifest records `skipped_candles` entries for all 10 affected instruments (candle[450] each)
  - [x] Bar count for each previously failing instrument: ≥89,438 bars — actual range 98,892–98,920 (99.5%), all pass
  - [x] `decisions/algorithm/2026-05-10-5min-data-coverage-both-universes.md` updated with final coverage: large-cap 15/15, midcap 48/48
- **Notes:** No code changes — operational fetch run only. Manifest did not need deletion; the 10 failing instruments were absent from the 5min completed list and fetched automatically. Skipped candle[450] is the same Zerodha tick-vs-OHLC aggregation artifact across all 10 instruments. Decision file updated with per-instrument bar counts and resolution notes. Unblocks TASK-0074 and TASK-0075 from data-availability perspective — both universes are now 100% complete for 5-min evaluation runs.

---

### [TASK-0100] Fix — OHLC bad-candle skip in `cmd/fetch-history`

- **Status:** done
- **Completed:** 2026-05-10
- **Priority:** high
- **Created:** 2026-05-10
- **Source:** bug
- **Context:** `cmd/fetch-history` hard-failed the entire instrument fetch on any OHLC validation error. 10 instruments (HDFCBANK, ICICIBANK + 8 midcap) hit candle[450] OHLC error (open 835.6 outside [low=837.4, high=843.8]) — a Zerodha tick-vs-aggregation artifact. Marcus ruling: warn, skip, continue.
- **Acceptance criteria:**
  - [x] `pkg/model/candle.go` Validate() unchanged — validator not relaxed
  - [x] `pkg/provider/zerodha/provider.go`: `*ErrBadCandles` typed warning; OHLC-invalid candles skipped, valid candles returned
  - [x] `cmd/fetch-history/main.go`: `fetchOne` logs per-candle warning, records `skipped_candles` in manifest, continues without failing (when ≥1 valid candle); all-bad case treated as failure
  - [x] Manifest records `{"index": N, "reason": "..."}` per skipped candle
  - [x] 90% completeness threshold passes (1 of ~98,900 candles skipped ≈ 0.001%)
  - [x] TDD: failing tests written before implementation
  - [x] `go1.25.0 test -race ./pkg/provider/zerodha/...` passes
  - [x] `go1.25.0 test -race ./cmd/fetch-history/...` passes
  - [x] `golangci-lint run ./pkg/provider/zerodha/...` passes
  - [x] `golangci-lint run ./cmd/fetch-history/...` passes
- **Notes:** 9 new tests. `ErrBadCandles` is a non-fatal typed warning — callers not using `errors.As` see a hard error (fail-safe). `CachedProvider` caches valid candles on `ErrBadCandles` but only when `len(candles) > 0` — empty results not cached (perspective review fix). `fetchOne` all-bad path returns error and skips completed mark (perspective review fix). `zerodha/cache` imports `zerodha` for the type check — not circular. `manifestSkippedCandle` is a local DTO mirroring `zerodha.SkippedCandle`. Unblocks: re-run `cmd/fetch-history` against 10 previously failing instruments (TASK-0101).

---

### [TASK-0099] Data — fetch and validate 5-min bar history for Nifty50 large-cap and Nifty Midcap 150 universes

- **Status:** done
- **Completed:** 2026-05-10
- **Priority:** high
- **Created:** 2026-05-10
- **Source:** session
- **Context:** ORB and Gap-and-Go evaluation runs need 5-min bar history for both universes. `cmd/fetch-history` supports `--timeframe 5min` but the 5-min cache had never been populated.
- **Acceptance criteria:**
  - [x] Run `cmd/fetch-history --universe universes/nifty50-large-cap.yaml --timeframe 5min --from 2021-01-01 --cache-dir .cache/zerodha`
  - [x] Run `cmd/fetch-history --universe universes/nifty-midcap-liquid.yaml --timeframe 5min --from 2021-01-01 --cache-dir .cache/zerodha`
  - [x] Document earliest available 5-min date per instrument for both universes in `decisions/algorithm/2026-05-10-5min-data-coverage-both-universes.md`
  - [x] Flag instruments with < 2 years history — none found; all succeeded instruments have 5+ years
  - [x] Bar count sanity verified: 99.5% of expected (98,900–98,921 bars vs 99,375 expected); all pass 90% threshold
  - [x] Session-boundary integrity checked: 09:15 IST open, 15:25 IST close confirmed; all anomalies are NSE calendar events (Muhurat, Budget day, election halts)
  - [x] Decision file includes minimum usable window (2021-01-01 to 2026-05-09 = 5.36 years), WF fold structure recommendation (2-yr IS / 6-month OOS / 6 folds), universe quality comparison
- **Notes:** Large-cap: 13/15 succeeded; HDFCBANK and ICICIBANK failed (OHLC validation error at candle[450] — Zerodha data quality issue, flagged for Marcus review). Midcap: 40/48 succeeded; 8 failed same error (ABCAPITAL, GODREJCP, SUNDRMFAST, ENDURANCE, LTTS, ALKEM, RATNAMANI, LEMONTREE). Kite's actual 5-min window is ~5 years (longer than the ~3-year estimate in task notes). Decision file: `decisions/algorithm/2026-05-10-5min-data-coverage-both-universes.md`.

---

### [TASK-0097] Eval — Nifty Midcap 150 correlation screen + kill-switch derivation (MACD crossover, 6 bootstrap survivors)

- **Status:** done
- **Completed:** 2026-05-09
- **Priority:** high
- **Created:** 2026-05-09
- **Source:** session
- **Context:** MACD crossover (17/26/9) passed the Nifty Midcap 150 bootstrap gate (TASK-0096): 6 of 25 WF survivors pass SharpeP5>0 AND P(S>0)>80%. Correlation screen, kill-switch threshold derivation, and portfolio construction recommendation.
- **Acceptance criteria:**
  - [x] Correlation screen: run `cmd/correlate` on the 6 survivors; flag |r| > 0.70 pairs (informational, not kill per pipeline convention)
  - [x] Kill-switch thresholds derived: 1.5x historical MaxDD per instrument, 2x historical MaxDDDuration — record per instrument
  - [x] Portfolio construction recommendation: vol-target at 10% annualized per instrument, Rs50k base notional, IT sector cap (PERSISTENT+COFORGE <=33%/Rs40k each when co-deployed)
  - [x] Results recorded in `decisions/algorithm/2026-05-09-macd-crossover-midcap-correlation-killswitch-portfolio.md`
- **Notes:** Correlation results: 0/15 pairs exceed 0.70 full-period; 0/15 exceed 0.60 in stress windows. Highest pair: PERSISTENT/COFORGE 0.3559 full-period (both IT sector). Kill-switch thresholds: PERSISTENT 5.62% MaxDD, TORNTPHARM 2.67%, COFORGE 12.57% (early-warning 8%), SUNDARMFIN 6.96%, INDHOTEL 8.70%, MUTHOOTFIN 6.27%. Source: `results/2026-05-09-TASK-0097/correlate-output.txt`, `results/2026-05-09-TASK-0096/` JSON files. Pipeline complete for MACD crossover Nifty Midcap 150.

---

### [TASK-0096] Eval — Nifty Midcap 150 bootstrap gate (MACD crossover, 25 walk-forward survivors)

- **Status:** done
- **Completed:** 2026-05-09
- **Priority:** high
- **Created:** 2026-05-09
- **Source:** session
- **Context:** Bootstrap validation on 25 WF survivors from TASK-0095. Gate: SharpeP5 > 0 AND P(Sharpe > 0) > 80% per instrument. Binary: `cmd/backtest --bootstrap --bootstrap-n 10000 --bootstrap-seed 42`.
- **Acceptance criteria:**
  - [x] Bootstrap run on all 25 WF survivors: `cmd/backtest --strategy macd-crossover --bootstrap --bootstrap-n 10000 --bootstrap-seed 42 --commission zerodha_full --macd-fast-period 17 --macd-slow-period 26 --macd-signal-period 9 --from 2018-01-01 --to 2025-01-01`
  - [x] Bootstrap gate applied: SharpeP5 > 0 AND P(Sharpe > 0) > 80% — 6/25 PASS
  - [x] Bootstrap stats recorded per instrument: SharpeP5, SharpeP50, SharpeP95, ProbPositiveSharpe, seed=42
  - [x] Correlation screening: deferred to TASK-0097 (6 survivors, sector-diverse — low prior risk)
  - [x] Results recorded in `decisions/algorithm/2026-05-09-macd-crossover-midcap-bootstrap-gate.md`
  - [x] 6 bootstrap survivors advance to TASK-0097
- **Notes:** Results: `results/2026-05-09-TASK-0096/bootstrap-results.csv`. Revisit-trigger activated: only 6/25 WF entrants survived bootstrap (WF was at exact gate boundary — as predicted). Anomalous WF cases (SCHAEFFLER, THOMASCOOK, EXIDEIND, LALPATHLAB) all killed by bootstrap. 6 survivors: PERSISTENT, TORNTPHARM, COFORGE, SUNDARMFIN, INDHOTEL, MUTHOOTFIN.

---

### [TASK-0095] Eval — Nifty Midcap 150 walk-forward (MACD crossover, 43 instruments)

- **Status:** done
- **Completed:** 2026-05-09
- **Priority:** high
- **Created:** 2026-05-09
- **Source:** session
- **Context:** MACD crossover (17/26/9) passed the Nifty Midcap 150 universe gate (TASK-0091): DSR avg Sharpe 0.0885, pass fraction 89.6% (43/48 instruments positive). Walk-forward validation ran on the 43 positive-Sharpe eligible instruments.
- **Acceptance criteria:**
  - [x] Walk-forward run: `cmd/walk-forward --instrument <each of 43> --from 2018-01-01 --to 2024-12-31 --strategy macd-crossover --macd-fast-period 17 --macd-slow-period 26 --macd-signal-period 9 --commission zerodha_full`
  - [x] Instrument-count gate applied: >= floor(0.60 * 43) = 25 instruments must pass walk-forward — PASS at exact boundary: 25/43
  - [x] Per-instrument verdict recorded: OOS Sharpe range +0.070 to +0.383 for survivors, 18 kills (10 NegativeFoldFlag, 6 OverfitFlag, 2 both)
  - [x] Walk-forward survivors (25 instruments) identified for bootstrap stage
  - [x] Results and verdict recorded in `decisions/algorithm/2026-05-09-macd-crossover-midcap-walk-forward.md`
  - [x] Gate passes: advancing to bootstrap (TASK-0096) on 25 surviving instruments
- **Notes:** Results file: `results/2026-05-09-TASK-0095/wf-results.csv`. [FLAGGED] exact gate boundary: 25 = floor(0.60 * 43) = 25. Anomalous cases (SCHAEFFLER, THOMASCOOK, EXIDEIND, LALPATHLAB) pass walk-forward gate — all have near-zero IS Sharpe producing extreme OOSISRatio, but OOS Sharpe positive and gate flags clear; flagged for bootstrap attention. Kill breakdown: 10 NegativeFoldFlag only, 1 OverfitFlag only (LICHSGFIN), 7 both. Survivor annotation for TASK-0096:
```json
{
  "survivor_input_from": "TASK-0095",
  "results_file": "results/2026-05-09-TASK-0095/wf-results.csv",
  "survivors": [
    {
      "strategy": "macd-crossover",
      "universe": "nifty-midcap-150",
      "instruments": ["NSE:PERSISTENT","NSE:TORNTPHARM","NSE:COFORGE","NSE:SUNDARMFIN","NSE:INDHOTEL","NSE:IPCALAB","NSE:MUTHOOTFIN","NSE:DEEPAKNTR","NSE:EIHOTEL","NSE:ENDURANCE","NSE:CUMMINSIND","NSE:FEDERALBNK","NSE:SAIL","NSE:RADICO","NSE:BALKRISIND","NSE:PIIND","NSE:PRESTIGE","NSE:BHEL","NSE:MHRIL","NSE:EXIDEIND","NSE:SCHAEFFLER","NSE:LEMONTREE","NSE:TATACONSUM","NSE:LALPATHLAB","NSE:THOMASCOOK"],
      "walk_forward_oos_sharpe_range": {"min": 0.070, "max": 0.383},
      "gate_boundary_flag": true,
      "parameters": {"fast": 17, "slow": 26, "signal": 9}
    }
  ]
}
```

---

### [TASK-0091] Eval — Nifty Midcap 150 universe sweep (MACD and other survivors)

- **Status:** done
- **Completed:** 2026-05-09
- **Priority:** high
- **Created:** 2026-05-07
- **Source:** session
- **Context:** `universes/nifty-midcap-liquid.yaml` is ready (TASK-0072 complete). The same evaluation pipeline used for Nifty50 large-caps (universe sweep → walk-forward → bootstrap) now needs to run on the midcap universe. MACD crossover (17/26/9) is the primary candidate given its strong large-cap results.
- **Acceptance criteria:**
  - [x] Universe sweep run: `cmd/universe-sweep --universe universes/nifty-midcap-liquid.yaml --strategy macd-crossover --from 2018-01-01 --to 2024-12-31 --timeframe daily --commission zerodha_full`
  - [x] ABCAPITAL check: returned insufficient_data=false (trade_count=53, Sharpe=0.655) — no replacement needed
  - [x] Gate applied: DSR avg Sharpe = 0.0885 > 0, pass fraction = 89.6% >= 40%, all 48 instruments >= 30 trades — PASS
  - [x] Marginal-ADV instruments (BHEL, SCHAEFFLER, EXIDEIND, NATIONALUM, SAIL) reviewed: all positive raw Sharpe (0.21–0.47), all sufficient — no replacements for universe v1
  - [x] Universe gate passes: advances to walk-forward on 43 positive-Sharpe eligible instruments
  - [x] Results and verdict recorded in `decisions/algorithm/2026-05-09-macd-crossover-midcap-universe-gate-passed.md`
- **Notes:** Results file: `results/2026-05-09-TASK-0091/midcap-sweep.csv`. 28 instruments pre-fetched via `cmd/fetch-history` before sweep (cache miss). LEMONTREE data from 2018-03-01 (IPO date). All 48 instruments returned sufficient_data=false. Survivor annotation for next pipeline task:
```json
{
  "survivor_input_from": "TASK-0091",
  "results_file": "results/2026-05-09-TASK-0091/midcap-sweep.csv",
  "survivors": [
    {
      "strategy": "macd-crossover",
      "universe": "nifty-midcap-150",
      "instruments": ["NSE:PERSISTENT","NSE:TORNTPHARM","NSE:COFORGE","NSE:SUNDARMFIN","NSE:INDHOTEL","NSE:IPCALAB","NSE:ABCAPITAL","NSE:LTTS","NSE:MUTHOOTFIN","NSE:DEEPAKNTR","NSE:EIHOTEL","NSE:MOTHERSON","NSE:ENDURANCE","NSE:CHOLAFIN","NSE:CUMMINSIND","NSE:FEDERALBNK","NSE:SAIL","NSE:RADICO","NSE:VINATIORGA","NSE:ALKEM","NSE:RATNAMANI","NSE:BALKRISIND","NSE:THERMAX","NSE:MPHASIS","NSE:M&MFIN","NSE:PIIND","NSE:NMDC","NSE:PRESTIGE","NSE:SUNDRMFAST","NSE:BHEL","NSE:MHRIL","NSE:ELGIEQUIP","NSE:EXIDEIND","NSE:SCHAEFFLER","NSE:LEMONTREE","NSE:COLPAL","NSE:TATACONSUM","NSE:LICHSGFIN","NSE:NATIONALUM","NSE:AJANTPHARM","NSE:GODREJCP","NSE:LALPATHLAB","NSE:THOMASCOOK"],
      "DSR_avg_sharpe": 0.088475,
      "pass_fraction": 0.8958,
      "parameters": {"fast": 17, "slow": 26, "signal": 9}
    }
  ]
}
```

---

### [TASK-0070] Tooling — `cmd/fetch-history` CLI for bulk intraday historical data

- **Status:** done
- **Completed:** 2026-05-09
- **Priority:** high
- **Created:** 2026-05-04
- **Source:** session
- **Context:** Zerodha Kite serves 5-min data back to ~2015. A one-shot CLI to drain full history for all instruments in a universe YAML — writing to the existing CachedProvider disk cache — enables all future intraday backtests to run from local files without a Zerodha token.
- **Acceptance criteria:**
  - [x] `cmd/fetch-history/main.go` CLI: flags `--universe`, `--timeframe` (repeatable), `--from`, `--cache-dir`, `--api-key`, `--access-token` (or KITE_API_KEY / KITE_ACCESS_TOKEN env vars)
  - [x] Reads universe YAML, iterates instruments × timeframes, calls `FetchCandles` for `[--from, today)`
  - [x] Writes results via `CachedProvider` so cache keys match what `cmd/backtest` and `cmd/universe-sweep` expect
  - [x] Incremental: `TODO(TASK-0080)` stub in `fetchOne` — full range fetch until TASK-0080 adds `LastCachedTime`
  - [x] Partial-failure recovery: `fetch-progress.json` manifest written atomically after each success; resume skips completed pairs
  - [x] Progress logging: `instrument × timeframe: fetched N candles [from → to]` per pair
  - [x] Dry-run flag `--dry-run`: prints plan without hitting the API; provider factory never called
  - [x] Auth flags and env var fallback covered by tests (mock provider via injected factory)
  - [x] Tests written before implementation (TDD); 12 tests covering dry-run, partial failure, resume, flag validation, env-var fallback, multiple timeframes
- **Notes:** `buildProductionProvider` not used `cmdutil.BuildProvider` — direct token injection for batch/CI use. `productionMode` global removed in quality-review round 1 — `providerFactory func(fetchFlags)` pattern used instead. TASK-0083 (`*ErrIncompleteData` handling) applies to this CLI but is tracked separately.

---

### [TASK-0063] Tooling — update `cmd/backtest` package doc comment to list all strategies

- **Status:** done
- **Completed:** 2026-05-08
- **Priority:** low
- **Created:** 2026-04-29
- **Source:** discovery
- **Context:** Package-level doc comment listed only `stub`, `sma-crossover`, `rsi-mean-reversion`. All strategy flag documentation was stale.
- **Acceptance criteria:**
  - [x] `cmd/backtest/main.go` doc comment updated — now lists all 7 strategies with their flag groups (`--fast-period/--slow-period`, `--rsi-period/--oversold/--overbought`, etc.)
  - [x] `golangci-lint run ./cmd/backtest/...` passes
- **Notes:** Resolved as part of the strategy wiring centralization refactor (2026-05-08). The doc comment was rewritten to list flag groups rather than just strategy names, which is more useful.

---

### [TASK-0079] Tech debt — centralized strategy registry

- **Status:** done
- **Completed:** 2026-05-07
- **Priority:** medium
- **Created:** 2026-05-05
- **Source:** discovery
- **Context:** Every new strategy required manual registration in 4+ CLI files. Six strategies already, two more incoming (TASK-0074, TASK-0075). Forgetting any registration produced silent wrong behaviour — strategy silently unavailable.
- **Acceptance criteria:**
  - [x] `internal/cmdutil/registry.go`: `StrategyRegistry` map type with `Register` and `MustGet` (panics on unknown name at startup)
  - [x] `internal/cmdutil/strategies.go`: single authoritative list of all strategy registrations
  - [x] `cmd/backtest`, `cmd/universe-sweep`, `cmd/walk-forward`, `cmd/sweep` all consume the central registry; local maps removed
  - [x] Adding a new strategy requires exactly one file change in one location; no `init()` auto-registration
  - [x] `TestStrategyRegistry` covers: known strategies return non-nil factory, unknown strategy panics with descriptive message, `ListStrategies()` returns sorted names
  - [x] `golangci-lint run ./...` and `go1.25.0 test -race ./...` pass
  - [x] Tests written before implementation (TDD)
- **Notes:** 8 strategies registered (stub + 7 live). Added `cci-mean-reversion` to `cmd/backtest` (was previously missing). Updated `cmd/walk-forward` tests for panic-based name validation. `cmd/sweep` validation gap (MustGet accepts cci-mean-reversion but local switch has no case) — pre-existing; tracked in TASK-0061.

---

### [TASK-0078] Infrastructure — session-boundary utilities for intraday strategies

**Completed:** 2026-05-07

- **Status:** done
- **Priority:** high
- **Created:** 2026-05-05
- **Source:** session
- **Context:** Both ORB (TASK-0074) and gap-and-go (TASK-0075) need to detect "is this the first bar of today's NSE session?" and "what was the close of the last bar of the previous session?" No utility existed for this. Without it, both strategies would independently hardcode IST timestamp parsing — duplicated, untestable, fragile on NSE holidays and half-days.
- **Acceptance criteria:**
  - [x] `pkg/strategy/session.go`: `IsSessionOpen(bar model.Candle) bool` — returns true if bar timestamp is 09:15 IST (first bar of NSE session)
  - [x] `pkg/strategy/session.go`: `PreviousSessionClose(bars []model.Candle, i int) (float64, bool)` — scans backward from index i to find last bar before 09:15 IST on a prior trading day; returns (close, true) or (0, false) if no prior session in slice
  - [x] IST timezone uses `time.FixedZone("IST", 5*3600+30*60)` — no tzdata dependency, consistent with engine convention
  - [x] Golden test: synthetic 5-min slice spanning 2 sessions — `IsSessionOpen` fires exactly once per day (09:15 bar only); `PreviousSessionClose` returns correct prior-session close from any intraday bar on day 2
  - [x] Weekend/holiday gap test: gap of >1 calendar day between sessions — `PreviousSessionClose` returns most recent prior-session close correctly
  - [x] `golangci-lint run ./pkg/strategy/...` passes
  - [x] Tests written before implementation (TDD)
- **Notes:** 11 tests (100% statement coverage, race-clean). Files: `pkg/strategy/session.go`, `pkg/strategy/session_test.go`. Decisions: IST package-level var via `time.FixedZone` (no tzdata), `//nolint:gocritic` for hugeParam on Candle value receiver (consistent with existing codebase). Unblocked TASK-0074 and TASK-0075 — all infrastructure dependencies for both strategies are now resolved; remaining blocker is Marcus rules only.

---

### [TASK-0059] Engine — walk-forward `Run()` factory API for stateful strategy wrappers

**Completed:** 2026-05-07

- **Status:** done
- **Priority:** high
- **Created:** 2026-04-27
- **Source:** session
- **Context:** `TimedExit` (added in TASK-0039) is stateful — it tracks `entryBar` and `inPosition` between `Next()` calls. The walk-forward harness previously accepted a single `strategy.Strategy` instance, safe only when stateless. Using `TimedExit` with walk-forward silently produced corrupted results across fold boundaries. Blocker for all intraday strategies using the TimedExit wrapper (ORB, gap-and-go).
- **Acceptance criteria:**
  - [x] `internal/walkforward.Run()` signature changed to accept `factory func() strategy.Strategy` instead of a single `strategy.Strategy` instance
  - [x] Each fold constructs a fresh strategy instance via `factory()` — no shared state across folds
  - [x] Existing callers updated: stateless strategies pass `func() strategy.Strategy { return myStrategy }` closures
  - [x] All 17 existing walk-forward tests still pass with race detector
  - [x] New test: `TimedExit`-wrapped strategy used in walk-forward — verify fold 2 starts with clean position state
  - [x] Godoc on `Run()` updated to remove the concurrent-safety caveat
  - [x] Tests written before implementation (TDD)
- **Notes:** `runFold()` calls `factory()` twice per fold (once IS, once OOS). `TestRun_TimedExitFoldStateIsolation` added — uses `alwaysBuyInner` + `NewTimedExit(maxHoldBars=3)` over 2 folds; verifies both folds non-degenerate (timer fires), proving no state bleed. Pre-existing gofumpt issue in `internal/output/output_test.go` fixed as opportunistic cleanup. Quality gate: PASS. 18 tests, 87.9% coverage, 0 lint issues.

---

### [TASK-0072] Data — Nifty Midcap 150 universe YAML

- **Status:** done
- **Completed:** 2026-05-07
- **Priority:** high
- **Created:** 2026-05-04
- **Source:** session
- **Context:** All strategies tested so far run only on Nifty50 large-caps. Midcap names have thinner analyst coverage, more retail participation, and more persistent behavioral inefficiencies. Same daily-bar pipeline applies, zero new infrastructure needed.
- **Acceptance criteria:**
  - [x] `universes/nifty-midcap-liquid.yaml` created in same format as `universes/nifty50-large-cap.yaml`
  - [x] 20–30 Nifty Midcap 150 names selected: continuous Kite daily bar history from 2018-01-01, reasonably liquid (ADV > ₹50 crore), no pending delistings or recent IPOs (25 instruments across 9 sectors; 5 marginal-ADV names flagged inline)
  - [x] Marcus (algo-trading-veteran) reviews final list for liquidity and data quality before first sweep run (approved 2026-05-07)
  - [x] `cmd/universe-sweep --universe universes/nifty-midcap-liquid.yaml` runs without errors (CLI confirmed 25 instruments parsed)
- **Notes:** ABCAPITAL flagged for Kite history verification (IPO 2017) on first sweep run. Unblocks TASK-0091.

---

### [TASK-0089] Tech debt — CachedProvider range-aware lookup (serve subset from disk, skip network)

- **Status:** done
- **Completed:** 2026-05-07
- **Priority:** medium
- **Created:** 2026-05-07
- **Source:** user
- **Context:** `CachedProvider.FetchCandles()` was keyed on exact `(instrument, timeframe, from, to)` tuples. If 2018–2024 was cached and the caller requested 2019–2022, it was a cache miss triggering a fresh Zerodha network call despite all data being on disk.
- **Acceptance criteria:**
  - [x] `CachedProvider.FetchCandles()`: before hitting network, scan cache dir for any file covering `(instrument, tf)`; if found file's `[cachedFrom, cachedTo]` fully contains `[from, to]`, deserialize and return the filtered slice — no network call
  - [x] Cache file index (or filename convention) encodes `from` and `to` so superset check requires only filename parsing, no file open
  - [x] On cache miss (no superset found): fetch from network, write full result to cache, return — existing behavior unchanged
  - [x] Superset check handles edge cases: exact match, `from` equal to cached start, `to` equal to cached end — all treated as hits
  - [x] No duplicate file written when a superset file already exists and covers the requested range
  - [x] Golden test: write a wide cache file (2018–2024); request a narrower range (2020–2022); assert zero network calls made and returned candles are within `[2020-01-01, 2022-12-31]`
  - [x] Existing `CachedProvider` tests still pass (exact-match path unchanged)
  - [x] `golangci-lint run ./pkg/provider/zerodha/cache/...` passes
  - [x] Tests written before implementation (TDD)
- **Notes:** Implemented via `findSupersetFile` (filename parsing, no file I/O per candidate), `filterCandles` (half-open `[from, to)` on `Candle.Timestamp`), `sanitizeInstrument` and `instrDir` (DRY refactor). 6 new tests, 90.8% coverage, 0 lint issues. Follow-up: TASK-0090 (corrupt-superset fallback test coverage).

---

### [TASK-0048] Tooling — weekly kill-switch monitor (`cmd/monitor`)

**Completed:** 2026-05-07

- **Status:** done
- **Priority:** high
- **Created:** 2026-04-25
- **Source:** session
- **Context:** Marcus specified weekly kill-switch monitoring (not quarterly re-validation). The existing `analytics.CheckKillSwitch()` and `DeriveKillSwitchThresholds()` functions were ready. This task wired them into a runnable binary that reads live trade history and thresholds, outputs alert status, and can be cron-scheduled.
- **Acceptance criteria:**
  - [x] Trade log file format decided and documented — `decisions/convention/2026-05-07-live-trade-log-json-array-format.md` (JSON array of model.Trade)
  - [x] `cmd/monitor/main.go` reads: (1) live trade log in JSON format, (2) kill-switch thresholds JSON via local thresholdsFile DTO
  - [x] Calls `analytics.CheckKillSwitch(recentTrades, liveCurve, thresholds)`
  - [x] Prints clear alert status: `OK`, `HALT (Sharpe breached)`, `HALT (drawdown breached)`, `HALT (duration breached)`
  - [x] Exit code 0 if OK, non-zero if any threshold breached
  - [x] Tests: 13 tests covering all threshold breach types, multiple simultaneous breaches, empty trade log, curve ordering, flag validation
- **Notes:** Implemented as pure file-reading CLI with no DataProvider dependency. Synthetic equity curve built from sorted trades by ExitTime. run() extraction pattern for testability. Quality gate: 85.9% coverage, 0 lint issues. Unblocked TASK-0056 (TASK-0048 dependency resolved). Follow-up cleanup: TASK-0088.

---

### [TASK-0086] Regime gate — compute per-regime Sharpe contributions for MACD survivors (deferred from TASK-0052)

**Completed:** 2026-05-07

- **Status:** done
- **Priority:** high
- **Created:** 2026-05-06
- **Source:** decision (regime gate deferred 2026-05-03; Marcus GO verdict evaluate session 2026-05-06)
- **Context:** Per-regime Sharpe contributions for SBIN and TITAN (survivors from correlation gate). Three regime windows: pre-COVID (2018-01-01 to 2020-01-31), COVID+recovery (2020-02-01 to 2021-06-30), post-recovery (2021-07-01 to 2024-12-31). Gate: any regime >= 70% → RegimeConcentrated=true → half-weight.
- **Acceptance criteria:**
  - [x] Per-regime trade logs extracted for each surviving instrument
  - [x] Per-trade Sharpe computed for each (instrument, regime) pair: `mean(ReturnOnNotional)/std(ReturnOnNotional)`, sample variance (n-1), no annualization
  - [x] `abs(S[regime]) / sum(abs(S[all regimes]))` computed for each instrument
  - [x] RegimeConcentrated flag set per instrument: true if any single regime >= 70% of absolute Sharpe mass
  - [x] Capital allocation adjustment documented: RegimeConcentrated=true → 50% of base allocation
  - [x] Results recorded in `decisions/algorithm/2026-05-07-regime-gate-results-sbin-titan-macd-task0086.md`
- **Notes:** Results: SBIN max contribution 41.84% (COVID+recovery), TITAN max contribution 45.97% (pre-COVID). Both RegimeConcentrated=false. No allocation adjustment. Both proceed to TASK-0087 at full ₹1,50,000 allocation. `--regime-gate` flag wired to `cmd/backtest` as part of this task (build + quality gate passed).

---

### [TASK-0085] Correlation — run pairwise Pearson r on all 6 MACD survivor pairs (equity curves)

**Completed:** 2026-05-07

- **Status:** done
- **Priority:** high
- **Created:** 2026-05-06
- **Source:** decision (Marcus GO verdict, evaluate session 2026-05-06)
- **Context:** Run pairwise correlation analysis on daily log-return equity curves for MACD crossover on SBIN, BAJFINANCE, TITAN, ICICIBANK. Compute 6 pairs: full-period (2018-2024) AND stress-period (COVID crash 2020-02-01 to 2020-06-30, rate-hike bear 2022-01-01 to 2022-12-31). Gate thresholds per correlation-gate decision (2026-04-27): full-period r < 0.7 AND both stress-period r < 0.6. Marcus expects SBIN/ICICIBANK to fail — both are Nifty Bank constituents, rate-sensitive, PSU+private banking. SBIN/BAJFINANCE borderline (NBFC, estimated full-period r 0.55-0.72). TITAN expected to pass all pairs. Apply DSR tiebreaker for failing pairs (SBIN DSR=0.7042 > BAJFINANCE=0.6120 > TITAN=0.6022 > ICICIBANK=0.3816).
- **Acceptance criteria:**
  - [x] Pairwise Pearson r computed for all 6 pairs on full-period (2018-2024) daily log-return equity curves
  - [x] Stress-period Pearson r computed separately for COVID crash (2020-02-01 to 2020-06-30) and rate-hike bear (2022-01-01 to 2022-12-31)
  - [x] Each pair labeled PASS or FAIL per gate (full-period r < 0.7 AND both stress-period r < 0.6)
  - [x] Tiebreaker applied for failing pairs: retain higher DSR-corrected Sharpe instrument (SBIN > BAJFINANCE > TITAN > ICICIBANK)
  - [x] Excluded instruments recorded with reason "excluded (correlation)" — not a gate failure
  - [x] Results feed directly into TASK-0087 (portfolio composition decision file)
- **Notes:** Daily equity curve analysis completed via `cmd/correlate` on MACD bootstrap survivors. Results file: `decisions/algorithm/2026-05-06-macd-correlation-gate-results-sbin-titan-survivors.md`. Stress window dates corrected in `internal/analytics/correlation.go` (COVID crash start: 2020-02-01, rate-hike end: 2022-12-31). All tests pass, lint clean. Result: SBIN and TITAN survive correlation gate; BAJFINANCE and ICICIBANK excluded. Unblocks TASK-0087.

---

### [TASK-0082] Tech debt — `cmd/backtest --bootstrap` output JSON missing distribution stats

**Completed:** 2026-05-06

- **Status:** done
- **Priority:** medium
- **Created:** 2026-05-05
- **Source:** session
- **Context:** `cmd/backtest --bootstrap` was printing bootstrap distribution stats (SharpeP5, SharpeP50, SharpeP95, ProbPositiveSharpe, WorstDrawdownP95) to stdout only. The output JSON written to `--out` contained only standard backtest metrics. The evaluation-run pipeline agent had to parse stdout to capture these stats during TASK-0069 — fragile, breaks if stdout format changes, and prevents machine-readable audit trails.
- **Acceptance criteria:**
  - [x] `internal/output/` result struct extended with bootstrap fields via `BootstrapStats` DTO under `"bootstrap"` JSON key — absent when bootstrap not run
  - [x] `cmd/backtest --bootstrap` populates these fields and writes them to the `--out` JSON
  - [x] Existing non-bootstrap JSON output unchanged (`omitempty` pointer suppresses key entirely)
  - [x] Stdout summary output unchanged — still printed as before
  - [x] `golangci-lint run ./...` and `go1.25.0 test -race ./...` pass
  - [x] Tests written before implementation (TDD) — 5 new tests
- **Notes:** Bootstrap fields placed under named `"bootstrap"` nested key (not top-level promotion) using `*BootstrapStats` with `omitempty` — avoids float64 zero-value ambiguity where 0.0 SharpeP5 is a valid result. Files: `internal/output/output.go`, `internal/output/output_test.go`. Quality gate: PASS, coverage 85.8%, lint clean, race clean.

---

### [TASK-0081] Bug — `zerodha.NewProvider` requires live token even when all candle data is cached

**Completed:** 2026-05-05

- **Status:** done
- **Priority:** high
- **Created:** 2026-05-05
- **Source:** session
- **Context:** `zerodha.NewProvider` unconditionally downloaded the instruments CSV via `kite.Instruments()` at construction time, requiring a valid Zerodha access token even when all requested candle data was already in the local disk cache. This blocked the TASK-0069 bootstrap session mid-run after token expiry.
- **Acceptance criteria:**
  - [x] Instruments CSV cached locally at `{cacheDir}/instruments.csv`; cache fresh if age < 24h
  - [x] `zerodha.NewProvider`: fresh cached CSV loads from disk, skips `kite.Instruments()` network call entirely
  - [x] Absent/stale cache fetches from Kite and writes result to disk before returning
  - [x] `FetchCandles` validates chunk completeness: returns `*ErrIncompleteData` if count < 90% of weekday estimate
  - [x] Existing `CachedProvider` path unchanged
  - [x] `golangci-lint run ./pkg/provider/...` and `go1.25.0 test -race ./pkg/provider/...` pass
  - [x] Tests written before implementation (TDD) — 10 tests written
- **Notes:** Completeness threshold set to 90% (revised from initial 95% post-build). Files: `pkg/provider/zerodha/instruments_cache.go` (new), `pkg/provider/zerodha/instruments_cache_test.go` (new), `pkg/provider/zerodha/errors.go`, `pkg/provider/zerodha/provider.go`, `pkg/provider/zerodha/provider_test.go`, `internal/cmdutil/cmdutil.go`. Quality gate: PASS, coverage 89.7%, lint clean, race clean.

---

### [TASK-0068] Evaluation — run SMA universe sweep and walk-forward at fast=20/slow=50

**Completed:** 2026-05-04

- **Status:** done
- **Priority:** high
- **Created:** 2026-05-04
- **Source:** session
- **Context:** TASK-0067 changed the --fast-period CLI default from 10 to 20. The evaluation run itself (universe sweep → universe gate → walk-forward per-instrument) ran with the new fast=20/slow=50 defaults. SMA fast=20/slow=50 is the re-test sanctioned by Marcus via pre-committed revisit trigger in the TASK-0053 kill decision.
- **Acceptance criteria:**
  - [x] Run: `go run ./cmd/universe-sweep --universe universes/nifty50-large-cap.yaml --strategy sma-crossover --from 2018-01-01 --to 2024-01-01 --commission zerodha_full > runs/universe-sweep-sma-20-50-2026-05-04.csv` (uses new fast=20, slow=50 defaults)
  - [x] Apply universe gate: DSR-corrected avg Sharpe > 0 AND ≥40% positive-Sharpe instruments with ≥30 trades
  - [x] Record universe gate outcome in `decisions/algorithm/2026-05-04-sma-crossover-fast20-slow50-universe-gate-failed.md`
  - [x] If universe gate fails: record kill decision and stop
  - [ ] If universe gate passes: run walk-forward per-instrument — NOT REACHED (gate failed)
  - [ ] Record walk-forward gate outcome in `decisions/algorithm/` — NOT REACHED
- **Notes:** OUTCOME: Universe gate failed. All 15 instruments have InsufficientData=true (trade_count 12–20, minimum required is 30). fast=20/slow=50 on daily bars generates only ~2–3 trades/year per instrument — too sparse for DSR or walk-forward validation. SMA crossover is now killed at two parameter settings: fast=10/slow=20 (instrument-count gate, TASK-0053) and fast=20/slow=50 (universe gate, TASK-0068). Do not revisit sma-crossover on daily bars unless a parameter set can be identified a priori that produces ≥30 trades/instrument. Kill decision: `decisions/algorithm/2026-05-04-sma-crossover-fast20-slow50-universe-gate-failed.md`. Results file: `runs/universe-sweep-sma-20-50-2026-05-04.csv`.

---

### [TASK-0053] Evaluation — walk-forward validation on universe sweep survivors

**Completed:** 2026-05-04

- **Status:** done
- **Priority:** high
- **Created:** 2026-04-25
- **Source:** session
- **Context:** Walk-forward tests whether fixed parameters remain stable across time periods not used for parameter selection. A strategy that survives the universe sweep but fails walk-forward on most of its passing instruments is overfitting to the historical regime, not capturing a transferable edge. Parameters are fixed — no reoptimization per fold.
- **Acceptance criteria:**
  - [x] Run walk-forward for each surviving strategy × instrument pair from TASK-0052, 2018-01-01 to 2024-01-01, 2yr IS / 1yr OOS / 1yr step
  - [x] Apply walk-forward gate (from TASK-0049): OverfitFlag = false AND NegativeFoldFlag = false
  - [x] A strategy must pass walk-forward on at least as many instruments as it passed the universe gate — if it passes universe on 8 instruments but walk-forward on only 3, the strategy is killed
  - [x] Record surviving strategy × instrument pairs with: AvgInSampleSharpe, AvgOutOfSampleSharpe, OOS/IS ratio, NegativeFoldCount
- **Notes:** Results: `runs/walk-forward-2026-05-04.csv` (26 rows, 14 MACD + 12 SMA instruments). OUTCOME: Both strategies killed at the instrument-count gate. macd-crossover: 9/14 instruments pass per-instrument walk-forward gate (needed 14). sma-crossover: 4/12 instruments pass (needed 12). Survivors: 0. Kill decisions recorded: `decisions/algorithm/2026-05-04-macd-crossover-walk-forward-instrument-count-gate.md`, `decisions/algorithm/2026-05-04-sma-crossover-walk-forward-instrument-count-gate.md`. MACD failures: NSE:RELIANCE/HINDUNILVR/WIPRO (OverfitFlag, OOSISRatio 0.34–0.48), NSE:TCS/HDFCBANK (NegFoldFlag, NegFoldCount=2). SMA failures: 8 of 12 instruments, dominated by simultaneous OverfitFlag+NegFoldFlag. Pipeline terminated — TASK-0054 cannot proceed without survivors. User decision required: (A) revisit instrument-count gate threshold, (B) revisit strategy parameters, or (C) accept kill and start fresh.

---

### [TASK-0052] Evaluation — universe sweep (cross-instrument primary gate)

**Completed:** 2026-05-03

- **Status:** done
- **Priority:** high
- **Created:** 2026-04-25
- **Source:** session
- **Context:** The primary validation gate for all six strategy families. A real edge should work on more than one instrument. Strategies that pass on RELIANCE only are not robust — they passed the wrong test. This run determines which strategies and instruments survive into walk-forward.
- **Acceptance criteria:**
  - [x] Run `cmd/universe-sweep` for all 6 strategies using plateau-midpoint parameters from TASK-0051, across all 15 instruments in `universes/nifty50-large-cap.yaml`, 2018-01-01 to 2024-01-01
  - [x] Apply universe gate (from TASK-0049): DSR-corrected average Sharpe > 0 AND ≥ 40% of instruments show positive Sharpe with ≥ 30 trades
  - [x] Strategies that fail the gate are killed — record kill decision in `decisions/algorithm/`
  - [x] Produce explicit survivor matrix: strategy × instrument (pass/fail per cell)
  - [x] Apply regime gate (from TASK-0049): no single regime accounts for > 70% of total Sharpe; strategies failing the regime gate are flagged even if they pass the universe gate
  - [x] Results saved to `runs/universe-sweep-YYYY-MM-DD.csv`
- **Notes:** Results: `runs/universe-sweep-2026-05-03.csv` (90 rows, original 6 strategies; 105 rows after CCI appended). Survivors: macd-crossover (DSRAvg=0.2715, 14/15 positive, eligible on 14 instruments) and sma-crossover (DSRAvg=0.0969, 12/14 positive, eligible on 12 instruments). Killed: donchian-breakout (DSRAvg=-0.1194, 7 sufficient instruments), rsi-mean-reversion (0 sufficient), bollinger-mean-reversion (0 sufficient), momentum (0 sufficient). Regime gate deferred — per-period trade logs not available in CSV; applies at portfolio construction (TASK-0055). Kill decisions recorded in decisions/algorithm/ (2026-05-03). Survivor metrics recorded in decisions/algorithm/ (2026-05-03). CCI mean-reversion (7th candidate, added post-hoc 2026-05-03 after signal audit 2026-05-02): killed at universe gate. DSRAvg=-0.0960, PassFraction=0.750, SufficientInstrumentCount=12. Raw AvgSharpe=+0.1996 but DSR correction with nTrials=12 and thin sample (~31–36 trades/instrument) pushed it negative. Full results in `runs/cci-universe-sweep-2026-05-03.csv`; CCI rows appended to `runs/universe-sweep-2026-05-03.csv`. Kill decision: `decisions/algorithm/2026-05-03-cci-mean-reversion-universe-gate-failed.md`. TASK-0052 now fully complete with all 7 strategies evaluated. TASK-0053 survivor list unchanged.

---

### [TASK-0060] Tooling — `--commission` flag for `cmd/universe-sweep`

**Completed:** 2026-05-01

- **Status:** done
- **Priority:** medium
- **Created:** 2026-04-27
- **Source:** discovery
- **Context:** `cmd/universe-sweep` hardcoded `CommissionZerodha` while `cmd/backtest` and `cmd/sweep` already had `--commission` via TASK-0051. `ParseCommissionModel` was already in `internal/cmdutil` — wiring it in was a small change.
- **Acceptance criteria:**
  - [x] `--commission` flag added to `cmd/backtest` (done 2026-04-29, TASK-0051)
  - [x] `--commission` flag added to `cmd/sweep` (done 2026-04-29, TASK-0051)
  - [x] `ParseCommissionModel` extracted to `internal/cmdutil` (done 2026-04-29, TASK-0051)
  - [x] `--commission` flag added to `cmd/universe-sweep`; wire `cmdutil.ParseCommissionModel` into `model.OrderConfig.CommissionModel`
  - [x] Invalid value returns a clear error at startup (`cmdutil.Fatalf("--commission: %v", err)`)
  - [x] Help text documents the accepted values (flag declaration and package doc comment updated)
- **Notes:** Cyclomatic complexity fix required: adding the commission error-check branch pushed `main()` to complexity 16 (max 15). Extracted `parseDateRangeAndTimeframe` helper to bring it back to ≤15. `golangci-lint run ./cmd/universe-sweep/...` clean. All tests pass with `-race`.

---

### [TASK-0051] Evaluation — in-sample baseline and parameter sensitivity (all 6 strategies, RELIANCE, 2018-2023)

**Completed:** 2026-05-01

- **Status:** done
- **Priority:** high
- **Created:** 2026-04-25
- **Source:** session
- **Context:** Orientation run on a single instrument to understand each strategy's behavior and find the robust parameter region via 1D sweep. Not a gate — the universe sweep is the gate. The output of this task is the plateau-midpoint parameter for each strategy, which is what gets used in the universe sweep.
- **Acceptance criteria:**
  - [x] Extend `cmd/backtest` and `cmd/sweep` to support all 6 strategies with `--commission` flag (done 2026-04-29; `ParseCommissionModel` extracted to `internal/cmdutil`)
  - [x] `internal/sweep.computePlateau` updated to apply 80% floor against valid-region peak (TradeCount ≥ 30) per Marcus's verdict; `SensitivityConcern` field added to `Report`
  - [x] Run `cmd/backtest` for all 6 strategies on NSE:RELIANCE 2018-01-01 to 2024-01-01 with `--commission zerodha_full` and default parameters
  - [x] Record for each: Sharpe, trade count, max drawdown in JSON files; bootstrap P5 for MACD (p5=-0.1465 at default fast=12, 75.3% prob positive); regime splits require `--curve-path` (not in original execution plan, non-blocking)
  - [x] Run 1D parameter sweep (`cmd/sweep`) for each strategy on its key parameter
  - [x] Identify plateau range (within 80% of peak Sharpe in ≥30-trade valid region) for each strategy
  - [x] Select plateau-midpoint parameter for each strategy for use in universe sweep; RSI, Bollinger, Momentum flagged `sensitivity_concern: true` (no valid ≥30-trade plateau on RELIANCE)
  - [x] Results saved to `runs/baseline-2026-04-30/` with JSON and sweep txt outputs; `plateau-params.json` produced
  - [x] Signal audit re-run with plateau-midpoint params (MACD fast=17, SMA slow=20, Donchian period=10) across 15 instruments → `runs/baseline-2026-05-01/signal-audit-plateau-params.csv`; sensitivity concerns confirmed: RSI/Bollinger/Momentum all cells excluded
- **Notes:** Plateau-midpoint params for universe sweep (TASK-0052): MACD fast=17/slow=26/signal=9; SMA fast=10/slow=20; Donchian period=10; RSI period=14 (sensitivity concern); Bollinger period=20 (sensitivity concern); Momentum lookback=231 (sensitivity concern). Signal audit results with plateau params: SMA 14/15 clear, MACD 15/15 clear, Donchian 7/15 clear; RSI/Bollinger/Momentum all excluded on all 15 instruments. `cmd/signal-audit/main.go` updated to use plateau-midpoint params — these are now the default hardcoded params for future audit runs.

---

### [TASK-0065] Evaluation — CCI mean-reversion signal frequency audit (pre-pipeline gate)

**Completed:** 2026-05-02

- **Status:** done
- **Priority:** high
- **Created:** 2026-05-02
- **Source:** session (Marcus iterate verdict on CCI mean-reversion evaluate session)
- **Context:** Marcus verdict was "iterate — run signal frequency audit before committing to full pipeline". CCI < -100 entry, cross above 0 exit, long-only, NSE large-caps, daily bars. Pass condition: avg ≥ 25 trades/instrument AND no instrument >30% COVID window (Q1-Q2 2020).
- **Acceptance criteria:**
  - [x] `strategies/ccimeanrev/` package created with `New(tf, period, entryThreshold, exitThreshold int)` — CCI < entryThreshold triggers Buy; CCI crossing above exitThreshold triggers Sell; long-only
  - [x] Uses `github.com/markcheno/go-talib` CCI indicator — no hand-rolled implementation
  - [x] `strategies/ccimeanrev/ccimeanrev_test.go` — TDD; covers signal fires on CCI < -100, exit fires on CCI cross above 0, no double-entry, exit threshold variants
  - [x] `cmd/signal-audit/main.go` extended with `cci-mean-reversion` in `allStrategyFactories` (period=20, entry=-100, exit=0)
  - [x] Signal frequency audit run across 15 NSE large-caps, 2018-01-01 to 2024-01-01
  - [x] Distribution report: avg=35.3 trades/instrument, max COVID%=14.7% (NSE:SBIN) — both pass
  - [x] PROCEED decision recorded in `decisions/algorithm/2026-05-02-cci-mean-reversion-signal-audit-proceed.md`
  - [x] `go1.25.0 test -race ./strategies/ccimeanrev/...` passes
  - [x] `golangci-lint run ./strategies/ccimeanrev/... ./cmd/signal-audit/...` passes
- **Notes:** Audit results: avg 35.3 trades/instrument (floor 25), 0/15 COVID clustering violations (floor 30%). Min instrument NSE:LT=29 trades. All 15 instruments cleared individually. `ApplyUniverseGate` implemented in `internal/universesweep` as part of this task.

---

### [TASK-0066] Tooling — build `cmd/walk-forward` CLI entrypoint

**Completed:** 2026-05-03

- **Status:** done
- **Priority:** high
- **Created:** 2026-05-03
- **Source:** session
- **Context:** `internal/walkforward/` was fully implemented with 17 tests but no CLI exposed it. TASK-0053 (walk-forward validation on 26 strategy×instrument pairs) could not run without a binary. This task wired the library to a runnable command.
- **Acceptance criteria:**
  - [x] `cmd/walk-forward/main.go` created accepting: `--strategy`, `--instrument`, `--from`, `--to`, `--is-years`, `--oos-years`, `--step-years`, `--commission`, and strategy-specific params
  - [x] Calls `internal/walkforward.Run()` with a strategy factory (`func() strategy.Strategy` — not a shared instance, per TASK-0059 intent)
  - [x] Outputs per-fold results (fold index, IS Sharpe, OOS Sharpe, OOS/IS ratio) and aggregate Report (OverfitFlag, NegativeFoldFlag, AvgISSharpe, AvgOOSSharpe, NegativeFoldCount) as JSON to stdout; optional CSV via `--out`
  - [x] Non-zero exit code when OverfitFlag or NegativeFoldFlag is set (enables scripting)
  - [x] Tests written before implementation (TDD)
  - [x] `golangci-lint run ./cmd/walk-forward/...` passes
  - [x] `go1.25.0 test -race ./...` passes
- **Notes:** Factory closures use dispatch table (map[string]strategyBuilder) with eager param validation at startup — closures cannot panic at fold-execution time. `run()` extracted from `main()` for testability (flag.NewFlagSet with ContinueOnError). Coverage: cmd/walk-forward 73.4%, internal/walkforward 88%. Quality gate: PASS 2026-05-03.

---

### [TASK-0064] Output — embed run metadata (instrument, timeframe, date range, params) in backtest result JSON and filename

**Completed:** 2026-05-01

- **Status:** done
- **Priority:** medium
- **Created:** 2026-05-01
- **Source:** discovery
- **Context:** Backtest output files contained only metrics with no metadata — no instrument, timeframe, date range, strategy parameters, or commission model. Filenames also omitted timeframe, so daily and intraday runs for the same strategy produced identically named files.
- **Acceptance criteria:**
  - [x] `internal/output` result type includes a `RunConfig` metadata block: `Instrument`, `Timeframe`, `From`, `To`, `Strategy`, `CommissionModel`, `Parameters` (map of key→value)
  - [x] `cmd/backtest` passes run config through to the output formatter; `cmd/sweep` logs run config at startup
  - [x] JSON output embeds the metadata block at the top level via `jsonResult` struct embedding (not nested under a sub-key); omitempty ensures zero-valued RunConfig emits no extra keys
  - [x] CLI `--out` flag default filename (when not overridden) includes timeframe: `{strategy}-{instrument}-{timeframe}-{from}-{to}.json` via `cmdutil.DefaultOutPath`
  - [x] `cmd/universe-sweep` logs run config (instrument count, timeframe, commission) to stderr at startup
  - [x] New tests added for `DefaultOutPath`, `RunConfig` top-level JSON embedding, zero-value backward compatibility; all tests pass, 0 lint issues
- **Notes:** `RunConfig` is a serialization DTO in `internal/output` (not `pkg/model`) — decoupled from domain type changes. `jsonResult` struct embeds both `RunConfig` and `analytics.Report` so Go's field promotion merges them at the JSON top level. Quality gate: PASS (build session 2026-05-01).

---

---

### [TASK-0054] Evaluation — Monte Carlo bootstrap on walk-forward survivors

**Completed:** 2026-05-05

- **Status:** done
- **Priority:** high
- **Created:** 2026-04-25
- **Source:** session
- **Context:** Bootstrap produces the confidence interval on the Sharpe estimate and the kill-switch Sharpe threshold.
- **Acceptance criteria:**
  - [x] Run `cmd/backtest --bootstrap` for each surviving strategy × instrument pair from TASK-0053, 10,000 simulations
  - [x] Apply bootstrap gate (from TASK-0049): SharpeP5 > 0 AND P(Sharpe > 0) > 80%
  - [x] Record for each survivor: SharpeP5, SharpeP50, SharpeP95, ProbPositiveSharpe, WorstDrawdownP95
  - [x] SharpeP5 value recorded as the kill-switch Sharpe threshold for that strategy × instrument pair
  - [x] Kill strategies failing the bootstrap gate; record kill decision in `decisions/algorithm/`
  - [x] Bootstrap seed logged with every result for reproducibility
- **Notes:** Completed under TASK-0069 remediation. Seed=42, 10k sims, zerodha_full. 4 MACD survivors: SBIN, BAJFINANCE, TITAN, ICICIBANK. Decision: decisions/algorithm/2026-05-05-macd-bootstrap-gate-results.md.

---

### [TASK-0069] Evaluation — reconsider instrument-count gate threshold for MACD at 17/26/9

**Completed:** 2026-05-05

- **Status:** done
- **Priority:** high
- **Created:** 2026-05-04
- **Source:** session
- **Context:** MACD crossover (fast=17, slow=26, signal=9) passed 9/14 instruments at walk-forward — 64% retention. Gate required 100% retention. Marcus ruled gate-design question.
- **Acceptance criteria:**
  - [x] Marcus reviews MACD's 9/14 WF pass pattern and rules on 60–70% instrument retention threshold
  - [x] Gate relaxed: document new threshold in `decisions/algorithm/`
  - [x] Bootstrap run for 9 passing instrument pairs
  - [x] Decision recorded with revisit trigger
  - [x] If NOT relaxed: record decision and kill MACD permanently
- **Notes:** Gate relaxed to 60% pipeline-wide. Bootstrap 10k sims seed=42. Survivors: SBIN, BAJFINANCE, TITAN, ICICIBANK. Decisions: decisions/algorithm/2026-05-05-walk-forward-instrument-count-gate-relaxed.md, decisions/algorithm/2026-05-05-macd-bootstrap-gate-results.md.

---

### [TASK-0087] Portfolio composition — record final portfolio, sizing, and kill-switch thresholds in decisions/algorithm/

**Completed:** 2026-05-07

- **Status:** done
- **Priority:** high
- **Created:** 2026-05-06
- **Source:** decision (Marcus GO verdict, evaluate session 2026-05-06; kill-switch derivation methodology 2026-04-21)
- **Context:** After correlation gate and regime gate are applied, record the final portfolio composition and all live-deployment parameters. Marcus's expected portfolio: NSE:SBIN + NSE:TITAN. Capital: ₹1.5 lakh notional per instrument (₹3 lakh total), no leverage. Vol-targeting: SizingVolatilityTarget, 20-bar rolling log-return std dev, fraction = volTarget/(instrumentVol × sqrt(252)), capped at 1.0.
- **Acceptance criteria:**
  - [x] `decisions/algorithm/YYYY-MM-DD-macd-portfolio-composition.md` created
  - [x] Final instrument list with inclusion/exclusion reason
  - [x] Capital allocation per instrument explicit: ₹ amount and fraction of total, adjusted for regime half-weight if applicable
  - [x] Vol-targeting sizing rule explicit: formula, 20-bar window, fraction cap at 1.0, no leverage, 10% annualized vol target
  - [x] Kill-switch thresholds per instrument: SharpeP5 threshold, MaxDD threshold (1.5× in-sample worst), MaxDDDuration threshold (2× in-sample worst)
  - [x] Regime gate outcome per instrument: RegimeConcentrated true/false, capital adjustment if applicable
  - [x] TASK-0056 (pre-live brief) unblocked after this file is written
- **Notes:** Done 2026-05-07. Decision file: decisions/algorithm/2026-05-07-macd-portfolio-composition.md. SBIN kill-switch: SharpeP5=0.0719, MaxDD=4.10%, MaxDDDuration=448 days. TITAN kill-switch: SharpeP5=0.0854, MaxDD=4.72%, MaxDDDuration=1,388 days. Both RegimeConcentrated=false; no allocation adjustment.

---

### [TASK-0055] Evaluation — cross-strategy correlation and portfolio construction

**Completed:** 2026-05-07

- **Status:** done
- **Priority:** high
- **Created:** 2026-04-25
- **Source:** session
- **Context:** Selects the final portfolio from bootstrap survivors. Two correlated strategies do not provide diversification — they add correlated risk, especially in the stress periods (2020, 2022). Portfolio at ₹3 lakh targets ~10% annualized vol using vol-targeting sizing per strategy.
- **Acceptance criteria:**
  - [x] Run `cmd/correlate` for all surviving strategy × instrument pairs from TASK-0054 (full equity curves)
  - [x] Apply correlation gate: full-period r < 0.7 AND stress-period r < 0.6 for every pair in the final portfolio
  - [x] If two strategies are correlated and from the same edge bucket, keep only the higher-DSR-Sharpe one
  - [x] Select 2-4 uncorrelated survivors for the portfolio
  - [x] Define capital allocation per strategy: combined portfolio targets ~10% annualized vol using `SizingVolatilityTarget`
  - [x] Record portfolio composition and sizing rule in `decisions/algorithm/`
  - [x] Record excluded strategies with reasons (correlation, gate failure, or sizing constraint)
- **Notes:** Final portfolio: NSE:SBIN + NSE:TITAN. Banking cluster (SBIN, ICICIBANK, BAJFINANCE) structurally correlated — BAJFINANCE and ICICIBANK excluded. Both instruments RegimeConcentrated=false (TASK-0086). Portfolio composition in decisions/algorithm/2026-05-07-macd-portfolio-composition.md. Sub-tasks: TASK-0085 (correlation gate), TASK-0086 (regime gate), TASK-0087 (composition file), TASK-0056 (pre-live brief).

---

### [TASK-0056] Evaluation — pre-live brief: kill-switch thresholds and go/no-go sign-off

**Completed:** 2026-05-07

- **Status:** done
- **Priority:** high
- **Created:** 2026-04-25
- **Source:** session
- **Context:** The final checkpoint before any live capital is allocated. Documents specific kill-switch thresholds, capital allocation, and the explicit go/no-go verdict for each portfolio strategy.
- **Acceptance criteria:**
  - [x] For each portfolio strategy: kill-switch thresholds recorded — SharpeP5 threshold (from TASK-0054), MaxDrawdownPct (1.5× in-sample worst), MaxDDDuration (2× in-sample worst)
  - [x] Thresholds written to `decisions/algorithm/YYYY-MM-DD-kill-switch-{strategy}.json` before first trade
  - [x] Capital allocation per strategy documented in ₹ and % of ₹3 lakh total
  - [x] Monitoring cadence documented: weekly kill-switch check via `cmd/monitor`
  - [x] Explicit go/no-go verdict per strategy: APPROVED FOR LIVE or NOT APPROVED with specific reason
  - [x] Algo reviewer acknowledgement noted in the brief
- **Notes:** Done 2026-05-07. NSE:SBIN APPROVED FOR LIVE (bootstrap p5=0.0719, 98.0% prob positive). NSE:TITAN APPROVED FOR LIVE (bootstrap p5=0.0854, 98.7% prob positive). Files: decisions/algorithm/2026-05-07-kill-switch-thresholds-live-brief.md, decisions/algorithm/2026-05-07-kill-switch-thresholds-sbin.json, decisions/algorithm/2026-05-07-kill-switch-thresholds-titan.json. Capital: ₹1.5L per instrument (50/50 split). Weekly Friday 17:00 IST check via cmd/monitor. Portfolio fully cleared for live deployment.

---

### [TASK-0071] Engine — verify overnight gap handling for intraday CNC backtests

**Completed:** 2026-05-07

- **Status:** done
- **Priority:** high
- **Created:** 2026-05-04
- **Source:** session
- **Context:** The engine processes bars sequentially. On 5-min bars with CNC overnight holds, the bar at 3:25 PM is immediately followed by 9:15 AM next day — a real-world 17-hour gap invisible to the event loop. Three concerns: (1) P&L must capture the overnight gap correctly (next open vs prior close); (2) stop-loss fills must use the gap-down open price, not the stop level; (3) any trade log metrics that assume uniform bar spacing may be wrong. Must be verified with a golden test before any intraday backtest result is trusted.
- **Acceptance criteria:**
  - [x] Read `internal/engine/` event loop and portfolio accounting: document exactly where P&L is computed and how fill price is determined for the bar following an overnight gap
  - [x] Write golden test: synthetic 5-min candle series spanning 2 sessions with a 3% gap-down open on day 2; position entered on day 1 close; verify P&L in trade log equals gap-adjusted loss, not zero
  - [x] Write golden test: synthetic strategy that signals position-close at bar N where bar N+1 opens with a 3% gap below the signal exit price — verify engine fills at bar N+1 open price, not at the signal price from bar N
  - [x] If bugs found: fix before any intraday strategy is evaluated; record fix in `decisions/`
  - [x] If engine already handles this correctly: record confirmed-correct note in `decisions/`
  - [x] Tests written before implementation (TDD)
- **Notes:** Done 2026-05-07. Engine is gap-transparent by construction — the `pendingSignal → candles[i].Open` fill path applies the pending fill to whatever Open the provider returns with no clamping, smoothing, or session-boundary check. Both golden tests passed immediately without any engine change. Decision recorded: `decisions/convention/2026-05-07-overnight-gap-fill-confirmed-correct.md`. New file: `internal/engine/engine_gap_test.go`. Quality gate: PASS (0 lint issues, all tests pass, 98.1% engine coverage).
