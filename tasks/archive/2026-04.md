# Task Archive — 2026-04

Completed and cancelled tasks from this month.

---

_Completed 2026-04-10_

### [TASK-0014] Analytics — annualized Sharpe ratio

- **Status:** done
- **Priority:** high
- **Created:** 2026-04-10
- **Source:** session
- **Context:** Win rate and max drawdown alone are insufficient to evaluate a strategy. Sharpe ratio is the primary signal-to-noise metric; without it, every backtest result is uninterpretable. Must be annualized correctly from the bar return series, not from total P&L.
- **Acceptance criteria:**
  - [x] `analytics.Report` gains a `SharpeRatio float64` field
  - [x] Sharpe computed from the equity curve's per-bar returns: `mean(r) / stddev(r) * sqrt(N)` where N is bars per year for the strategy's timeframe
  - [x] Annualization factor: daily bars → 252, 15min NSE bars → 252*25, 5min → 252*75, 1min → 252*375, weekly → 52
  - [x] Handles edge cases: fewer than 2 returns (< 3 equity points), zero variance (constant equity), unknown timeframe
  - [x] Tests: 11 table-driven cases including all timeframes, edge cases, negative Sharpe, and hand-verified expected values (e.g. returns [+0.1, -0.1, +0.1] daily → Sharpe = √21)
  - [x] `output.printSummary` updated to include Sharpe ratio in the printed report
- **Notes:** `sharpeAnnualizationFactor` reached 100% coverage. `analytics` package at 96.3% overall.

---

**Completed: 2026-04-10**

### [TASK-0013] Analytics — equity curve time series in Portfolio

- **Status:** done
- **Priority:** high
- **Created:** 2026-04-10
- **Source:** session
- **Context:** Sharpe ratio and rolling drawdown require a time series of portfolio equity value, not just per-trade P&L. The Portfolio currently has no mechanism to snapshot equity at each bar; this task adds it.
- **Acceptance criteria:**
  - [x] `Portfolio` tracks an `EquityCurve []EquityPoint` where each point is `{Timestamp time.Time; Value float64}`
  - [x] `EquityPoint.Value` = cash + mark-to-market value of any open position at the current bar's close price
  - [x] Engine calls a `portfolio.RecordEquity(candle)` method after each bar (open positions marked at close)
  - [x] `Portfolio.EquityCurve()` returns the full time series after Run completes
  - [x] Tests: known candle/signal sequence → expected equity curve values (table-driven)
  - [x] No allocations per-bar beyond the pre-allocated slice (grow-once strategy)
- **Notes:** `EquityPoint` added to `pkg/model/equity.go`. `equityCurve` pre-allocated with `len(candles)` capacity in `newPortfolio`. `RecordEquity` marks positions against `candle.Instrument` only (correct for single-instrument v1). TASK-0014 (Sharpe) is now unblocked.

---

<!-- Tasks are appended here when marked done or cancelled. Format: -->
<!-- **Completed/Cancelled: YYYY-MM-DD** -->
<!-- ### [TASK-NNNN] Title -->
<!-- ... full task block ... -->

**Completed: 2026-04-02**

### [TASK-0001] Project scaffolding — module, folder structure, and interface contracts

- **Status:** done
- **Priority:** critical
- **Created:** 2026-04-01
- **Source:** project
- **Context:** The three original tasks (module init, DataProvider interface, Strategy interface) were each half-a-day items. Merged here because interface design isn't mechanical — it takes real deliberation to get the signatures right, and you want to see the whole contract in one sitting before committing.
- **Acceptance criteria:**
  - [x] `go mod init github.com/vikrantdhawan/backtesting-algo-trading` run with correct module path
  - [x] Full folder structure created: `cmd/backtest/`, `internal/engine/`, `internal/analytics/`, `internal/output/`, `pkg/strategy/`, `pkg/provider/`, `pkg/model/`, `strategies/`, `testdata/`, `decisions/`, `sessions/`
  - [x] Dependencies added: `github.com/markcheno/go-talib`, `github.com/stretchr/testify`
  - [x] `DataProvider` interface defined in `pkg/provider/provider.go` with `FetchCandles` and `SupportedTimeframes` — interface only, no impl
  - [x] `Strategy` interface defined in `pkg/strategy/strategy.go` — receives candles, emits signal, declares required timeframe and lookback
  - [x] Compile-time interface check tests for both interfaces
  - [x] `go test ./...` passes (empty packages are fine; no red build)
- **Notes:** Spend time on interface signatures — these are the load-bearing walls. Wrong signatures here means pain in every downstream package.

---

**Completed: 2026-04-02**

### [TASK-0002] Core domain types in pkg/model

- **Status:** done
- **Priority:** critical
- **Created:** 2026-04-01
- **Source:** project
- **Context:** Every other package imports pkg/model. Must be stable before engine or provider work starts.
- **Acceptance criteria:**
  - [x] `Candle` — OHLCV + instrument string + timestamp, with a constructor and basic validation
  - [x] `Timeframe` — typed enum (1min, 5min, 15min, daily, weekly) with `String()` and `Duration()` methods
  - [x] `Signal` — typed enum (buy, sell, hold) with `String()`
  - [x] `Position` — instrument, quantity, entry price, direction (long/short)
  - [x] `Trade` — completed round-trip: entry/exit price, quantity, direction, instrument, timestamps, realized P&L field
  - [x] `OrderConfig` — slippage %, commission model (flat fee vs percentage)
  - [x] Table-driven unit tests for all non-trivial methods (Timeframe.Duration, Candle validation, Trade P&L)
- **Notes:** Every type must carry an instrument identifier. Trade's realized P&L is a field, not computed on the fly — analytics reads it directly.

---

**Completed: 2026-04-02**

### [TASK-0003] Engine — event loop and candle feeding

- **Status:** done
- **Priority:** high
- **Created:** 2026-04-01
- **Source:** project
- **Context:** First slice of the engine. Focuses purely on the loop: pulling candles, handing them to the strategy one bar at a time, collecting signals. No order execution yet — signals just get logged.
- **Acceptance criteria:**
  - [x] `Engine` struct in `internal/engine/` with `EngineConfig` (initial cash, slippage, commission, position size)
  - [x] `Engine.Run(provider DataProvider, strategy Strategy) error` method
  - [x] Candles fetched once from provider, then fed to strategy one bar at a time
  - [x] No-lookahead enforced: strategy receives `candles[:i+1]` at bar `i`, never future bars
  - [x] Strategy's declared lookback period respected — loop starts at `lookback` bars in
  - [x] Signal collected each bar and stored for next phase (order execution)
  - [x] Tested with a stub `DataProvider` (returns hardcoded candles) and stub `Strategy` (always returns Buy)
- **Notes:** No real order execution in this task — signals are collected but not acted on. That's TASK-0004.

---

**Completed: 2026-04-03**

### [TASK-0004] Engine — portfolio state and trade log

- **Status:** done
- **Priority:** high
- **Created:** 2026-04-01
- **Source:** project
- **Context:** Wires signals to portfolio state: open/close positions, track cash, accumulate the trade log. No slippage or commission yet — clean fills at stated price. Execution realism comes in TASK-0005.
- **Acceptance criteria:**
  - [x] `Portfolio` type tracking: current cash, open positions (map[instrument]Position), closed trades ([]Trade)
  - [x] Signal-to-order translation: Buy → open long position, Sell → close position (no shorting in v1)
  - [x] Cash debited on open, credited on close; guard against buying more than available cash
  - [x] Trade log populated with each closed trade (entry/exit price, quantity, instrument, timestamps)
  - [x] Tests: specific signal sequence → specific portfolio states and trade log entries
  - [x] Test: no position opened if cash insufficient
- **Notes:** Fills at the stated price here — slippage and commission added in TASK-0005. Keeps concerns separated and testable independently.

---

**Completed: 2026-04-04**

### [TASK-0005] Engine — order execution model (fills, slippage, commission, benchmark)

- **Status:** done
- **Priority:** high
- **Created:** 2026-04-01
- **Source:** project
- **Context:** Adds execution realism on top of TASK-0004. The fill model, slippage, and commission all live here. Also includes the benchmark test for the hot loop.
- **Acceptance criteria:**
  - [x] Market orders fill at next candle's open, not current close — enforced via pending-signal buffer in engine.go
  - [x] Slippage applied at fill: configurable fixed percentage (e.g. 0.05%), deducted from fill price
  - [x] Commission applied per trade: configurable flat fee or percentage; Zerodha default baked in (₹20 or 0.03% whichever is lower)
  - [x] Trade's realized P&L computed after commission and slippage
  - [x] Tests: known candle sequence → exact expected fill prices and P&L values (table-driven, 6 cases)
  - [x] Benchmark: `BenchmarkEngineRun` in `internal/engine/engine_bench_test.go` processing 10 years of daily candles — 256µs/op (budget: 1ms)
- **Notes:** With this task done, the engine is functionally complete for v1. TASK-0003 + 0004 + 0005 together = the original monolithic TASK-0005.

---

**Completed: 2026-04-06**

### [TASK-0006] Analytics — P&L, win rate, and max drawdown

- **Status:** done
- **Priority:** medium
- **Created:** 2026-04-01
- **Source:** project
- **Context:** Standalone package that takes a trade log and computes performance metrics. No engine reference — pure function over []Trade.
- **Acceptance criteria:**
  - [x] `internal/analytics/` package with `Compute(trades []Trade) Report` function
  - [x] `Report` struct: TotalPnL, WinRate (%), MaxDrawdown (%), TradeCount, WinCount, LossCount
  - [x] MaxDrawdown computed correctly from equity curve (not just single-trade losses)
  - [x] Tests with known trade sequences and hand-verified expected metric values
  - [x] Edge cases tested: empty trade log, all winners, all losers, single trade
- **Notes:** 7 tests covering empty/single-winner/single-loser/all-winners/all-losers/mixed/breakeven. Breakeven counts as LossCount. MaxDrawdown is 0 when equity never goes positive (no measurable peak).

---

**Completed: 2026-04-09**

### [TASK-0009] Zerodha provider — local caching layer

- **Status:** done
- **Priority:** medium
- **Created:** 2026-04-01
- **Source:** project
- **Context:** Iterative backtesting means the same candle data gets fetched repeatedly. A local cache avoids API hammering and speeds up development cycles significantly.
- **Acceptance criteria:**
  - [x] Cache layer wraps the Zerodha fetcher transparently — DataProvider interface unchanged
  - [x] Cache hit returns stored candles without an API call
  - [x] Cache miss fetches from API, stores result, returns candles
  - [x] Cache key includes: instrument, timeframe, date range
  - [x] Cache invalidation: TTL-based (1h) for recent data; historical ranges never expire
  - [x] Tests: verify cache hit prevents API call (mock verifies zero API calls on second fetch)
- **Notes:** `pkg/provider/zerodha/cache/` — `CachedProvider` decorator. Verified end-to-end against live Kite API via `cmd/providertest` (miss then hit, 3-5x speedup).

---

**Completed: 2026-04-09**

### [TASK-0008] Zerodha provider — auth, token refresh, and FetchCandles

- **Status:** done
- **Priority:** medium
- **Created:** 2026-04-01
- **Source:** project
- **Context:** First concrete DataProvider implementation. Auth and basic fetch are the hard part — get these right before building the cache layer on top.
- **Acceptance criteria:**
  - [x] `pkg/provider/zerodha/` package implementing `DataProvider` interface (compile-time check)
  - [x] Auth: daily token refresh handled internally — not exposed through the interface
  - [x] `FetchCandles` implemented with pagination/chunking (strategy from TASK-0007 analysis)
  - [x] Respects rate limits (strategy from TASK-0007 analysis)
  - [x] Mock-based test suite: no real Zerodha API calls in any test
  - [x] Integration test with a recorded API response fixture in `testdata/`
- **Notes:** Auth strategy and pagination decisions in `decisions/infrastructure/`. Token persisted to `~/.config/backtest/token.json` with 6AM IST expiry. Chunk strategy: per-interval maxDays map, 350ms sleep between chunks.

---

**Completed: 2026-04-09**

### [TASK-0010] Output package — result formatting and JSON export

- **Status:** done
- **Priority:** medium
- **Created:** 2026-04-07
- **Source:** project
- **Context:** `internal/analytics/` produces a `Report`; `internal/output/` needs to format it for human consumption and persist it as JSON. Without this, backtest results only exist in memory.
- **Acceptance criteria:**
  - [x] `internal/output/` package with `Write(report analytics.Report, cfg OutputConfig) error`
  - [x] Human-readable text summary printed to stdout (P&L, win rate, max drawdown, trade count)
  - [x] JSON export to a configurable output file path
  - [x] `OutputConfig` specifies: output file path, whether to print to stdout
  - [x] Tests: known `Report` → expected JSON output (table-driven)
  - [x] Edge case: empty report (zero trades) produces valid output, not a crash

---

**Completed: 2026-04-07**

### [TASK-0007] [ANALYSIS] Zerodha API — auth flow, rate limits, and historical data constraints

- **Status:** done
- **Priority:** high
- **Created:** 2026-04-01
- **Source:** discovery
- **Context:** Before writing a line of Zerodha provider code, we need to know what we're actually building against. The auth flow, rate limits, max candles per request, and historical data depth all affect the implementation design. This is an analysis-only task — the output is a decision doc, not production code.
- **Acceptance criteria:**
  - [x] Document written in `decisions/` covering: historical data depth per timeframe, rate limits (requests/sec and daily), max candles per API request, timeframes actually supported
  - [x] Auth flow documented: how the daily access token refresh works end-to-end (login URL, request token, exchange for access token)
  - [x] Pagination/chunking strategy decided: how to split large date ranges into valid API requests
  - [x] Caching strategy decided: file-based vs in-memory, cache key structure, invalidation approach
  - [x] Scratch prototype of auth flow verified to work (not production-quality — just proves the mechanism)
- **Notes:** Six decisions recorded (auth strategy, pagination, cache, instrument token lookup CSV, TimeframeWeekly exclusion, stdlib dotenv). Auth prototype at cmd/authtest/main.go. Unblocked TASK-0008 and TASK-0009.
